# 🌍 Проєкт "Подорожники" (Travelers)

Професійна платформа для мандрівників, де користувачі можуть ділитися своїми історіями, переглядати
публікації інших та зберігати цікавий контент.

---

## 🛠 Технологічний стек

- **Framework:** Next.js (App Router)
- **State Management:** Zustand
- **Data Fetching:** TanStack Query (React Query) + Axios
- **Forms:** Formik + Yup
- **Styling:** CSS Modules
- **Icons:** SVG Sprite system

---

## 📁 Структура проекту (Root Level)

Проєкт організований без папки `src`. Усі ключові директорії розташовані в корені:

- **/app** — Маршрути, сторінки, API-проксі та глобальні стилі.
- **/components** — Реалізація UI Kit та бізнес-компонентів.
- **/lib** — Конфігурація API (Axios), Zustand-сховища та константи.
- **/types** — TypeScript інтерфейси (User, Story).
- **/public** — Статичні ресурси (включаючи `/sprites/sprite.svg`).

---

## 📱 Стандарти верстки (Mobile First)

Верстка виконується за принципом **Mobile First** (спочатку стилі для мобілки, потім медіа-запити на
розширення).

### Точки перелому (Breakpoints):

- **Mobile (від 320px):** Гумова верстка, адаптивна з **375px**.
- **Tablet (від 768px):** Адаптивна верстка.
- **Desktop (від 1440px):** Адаптивна верстка.

### Технічні правила:

1. **Стилізація:** Лише CSS Modules (`*.module.css`) у форматі `camelCase`.
2. **Іконки:** Використання єдиного спрайту. _Приклад:_
   `<use href="/sprites/sprite.svg#icon-heart" />`
3. **Зображення:** Використання `next/image` для автоматичної оптимізації.

---

## 📊 Логіка контенту (Пагінація та Сортування)

Кількість карток (`perPage`) змінюється залежно від пристрою розробника:

| Розділ                                | ПК (1440+) | Планшет (768+) | Мобіл (375+) | Логіка / Сортування                  |
| :------------------------------------ | :--------: | :------------: | :----------: | :----------------------------------- |
| **Профіль / Збережені / Мої історії** |   **6**    |     **4**      |    **4**     | `Load More`. Популярність ➡️ Дата    |
| **Всі історії (Blog)**                |   **3**    |     **4**      |    **2**     | Без підвантаження. Найпопулярніші    |
| **Мандрівники (Team)**                |   **12**   |     **8**      |    **8**     | `Load More`. За кількістю публікацій |
| **Історії мандрівників (Public)**     |   **9**    |     **8**      |    **9**     | `Load More`. За популярністю         |
| **Головна: Популярні історії**        |   **3**    |     **4**      |    **3**     | Без підвантаження. За популярністю   |
| **Головна: Наші мандрівники**         |   **4**    |     **4**      |    **4**     | Без підвантаження. За публікаціями   |

---

## 🚦 Система роутингу (Route Groups)

1. **`(auth routes)`**: `/sign-in`, `/sign-up`. Містять логіку `router.refresh()` для оновлення
   сесії.
2. **`(private routes)`**: `/profile`, `/stories/create`, `/saved`, `/my-stories`. Захищені
   Middleware.
3. **`(public routes)`**: Головна сторінка, стрічка історій, список мандрівників, публічні профілі.
4. **`/app/api`**: Проксі-шар для безпечної взаємодії з бекендом та обходу CORS.

---

## 📡 Взаємодія з даними

### Server-side (`/lib/api/serverApi.ts`)

Використовується в серверних компонентах (`page.tsx`) для SSR. Передає куки для перевірки сесії на
стороні сервера.

### Client-side (`/lib/api/clientApi.ts`)

Використовується в клієнтських компонентах та формах. Запити йдуть через проксі-роути `/api/...`.
Для завантаження фото обов'язково використовувати `FormData`.

---

## 💡 Інструкції для команди

- **Proxy:** Файл `proxy.ts` у корені контролює доступ до приватних зон.
- **Global State:** Використовуйте `useAuthStore` для миттєвого оновлення інтерфейсу користувача.
- **Modals:** Усі модальні вікна мають рендеритись через `ModalLayout.tsx` за допомогою React Portal
  у `div#modal-root`.
- **SEO:** Мета-дані сторінок налаштовуються через константи в `/lib/constants/seo.ts`.
